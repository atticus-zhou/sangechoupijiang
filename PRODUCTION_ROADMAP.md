# 三省六部生产化路线

本文记录把当前多 Agent 原型推进到生产级平台的工程路线。目标不是让系统一次性变复杂，而是逐层补上生产环境必需的约束。

## 当前定位

当前项目已经具备多 Agent 协作平台的骨架：

- 中书省负责计划生成。
- 门下省负责计划审议。
- 尚书省负责调度决策。
- 六部负责记忆、资源、通信、执行、验证和产出。
- Web 层提供任务提交、配置管理、历史查看和产物下载。

现阶段更接近可运行原型。要进入生产级，需要重点提升可控性、可恢复性、可观测性和验证能力。

## 已开始的生产化底座

### 办公室配置

系统开始引入 `Office` 概念。办公室不是新的一批 Agent，而是同一批 Agent 的场景化工作环境。

第一批内置办公室：

- `research`: 研究报告办公室。

研究报告办公室定义了：

- 每个 Agent 在研究报告场景里的职责。
- 预期产物类型，如报告、数据表、图表、截图和来源清单。
- 验收标准，如近期数据、来源可追踪、报告可交付。

为什么重要：

多 Agent 工作台不能只靠“通用任务”。办公室让同一套 Agent 能进入不同工作模式，后续可以继续扩展 AI 漫剧办公室、代码办公室、知识库办公室等。

### 任务生命周期持久化

任务现在应具备可持久化的运行记录：

- `queued`: Web API 已接受任务。
- `running`: 后台任务已开始。
- `completed`: 任务成功完成。
- `failed`: 任务失败并记录错误。
- `finished`: 非成功但已结束的状态。

相关数据保存在 SQLite：

- `task_runs`: 当前/历史任务运行状态。
- `task_events`: 任务事件日志。
- `task_history`: 兼容已有历史记录页面。
- `workspaces`: 项目空间。
- `artifacts`: 工作空间内的产物。

为什么重要：

生产系统不能只依赖内存状态。任务可能耗时较长，服务可能重启，用户也需要追踪“任务到了哪一步、为什么失败、产物是什么”。

## 下一批优先事项

### 1. 明确主架构

当前存在两套核心：

- `src/`: Web 平台主线。
- `sanshengliubu/`: SDK/会话式主线。

建议选择 `src/` 作为生产平台主线，将 `sanshengliubu/` 逐步定位为兼容 SDK 或归档旧实现。

为什么重要：

双核心会让功能、修复和文档分裂。生产系统必须有一个明确的事实来源。

### 2. 引入任务队列

当前 Web 层使用后台协程启动任务，适合演示但不适合生产。

建议演进路径：

- 第一阶段：SQLite 任务队列 + worker。
- 第二阶段：Redis Queue、Arq、Dramatiq 或 Celery。
- 第三阶段：如需要复杂长流程，再考虑 Temporal 等工作流引擎。

为什么重要：

任务队列可以提供重试、取消、超时、并发限制、失败恢复和横向扩展能力。

### 3. 强化结构化输出校验

所有 Agent 的关键输出都应经过 schema 校验：

- 计划必须至少包含一个步骤。
- 调度决策必须引用存在的步骤。
- 部门名称必须来自白名单。
- 验证结果必须是明确的 pass/fail。

为什么重要：

LLM 输出会漂移。生产级 Agent 系统需要用代码约束流程边界。

### 4. 强化刑部验证

当前验证偏轻量，后续应按任务类型引入专门验证器：

- 报告类：来源数量、年份、结构完整性、空洞表达比例。
- 代码类：单元测试、静态检查、格式化检查。
- 文件类：文件存在性、大小、内容格式。
- 搜索类：来源可信度、重复来源、时间有效性。

为什么重要：

多 Agent 的关键不是“能生成”，而是“能判断生成结果是否可靠”。

### 5. 建立评估集

建议维护一批固定任务，用来衡量改动效果：

- 简单报告任务。
- 多来源研究任务。
- 代码修改任务。
- 模糊需求任务。
- 故意失败任务。

记录指标：

- 成功率。
- 平均耗时。
- 平均 token/成本。
- 验证通过率。
- 人工介入次数。

为什么重要：

没有评估集，prompt 和流程改动只能靠感觉判断。

### 6. 工具权限和审计

工具应分级：

- 只读工具。
- 联网工具。
- 写文件工具。
- 执行命令工具。
- 高风险外部系统工具。

每次工具调用都应记录参数、结果、调用 Agent 和耗时。

为什么重要：

一旦 Agent 可以真实行动，权限和审计就是平台可信度的基础。

### 7. Prompt 版本化

每次修改系统提示词都应生成版本，任务记录保存使用过的 prompt 版本。

为什么重要：

Agent 行为很大程度由 prompt 决定。没有版本，历史结果难以复现，质量波动也难以定位。

## 产品问题

继续推进前，需要逐步明确这些问题：

1. 这个平台的第一主场景是什么：研究报告、代码任务、企业知识库，还是通用 Agent 工作台？
2. 用户更需要全自动完成，还是“人能随时介入”的可控流程？
3. 产物最重要的是报告、代码改动、数据表格，还是可下载的综合工作包？
4. 未来是单人本地工具，还是多人团队平台？

这些问题会影响 UI、权限、队列、存储和 Agent 分工。

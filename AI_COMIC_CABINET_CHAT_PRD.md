# AI 漫剧办公室：内阁对话层 PRD

版本：v0.1  
日期：2026-06-03  
范围：仅覆盖 AI 漫剧办公室中“灵感到故事确认”的内阁对话体验，不覆盖后续人物、道具、场景、分镜、提示词包和 Word 交付的完整生产链路。

## 1. 背景

当前 AI 漫剧办公室已经具备办公室隔离、模型配置、部门分工、剧本预览和后续生产入口，但“内阁”体验仍然偏机械。

用户输入一个灵感后，系统容易输出类似会议纪要、结构化评审、模板化追问的内容，例如“阶段”“完整度”“主冲突”“首辅收束”“每集梗概”等。这些信息对后台协作有价值，但不适合直接作为创作者面对的主界面。

用户真正想要的是一个像 AI 对话框一样的主创助手：能听懂灵感，顺着人的表达继续追问，帮助用户把模糊想法变成一个可以继续生产的完整故事。

## 2. 产品目标

把“内阁”从用户可见的会议系统，重构为用户可见的故事对话系统。

用户看到的应该是：

- 一个懂故事的主创助手。
- 一问一答的自然对话。
- 少量、贴合当前灵感的问题。
- 一版可读、完整、有情绪的故事提案。
- 用户确认后再进入后续生产。

用户不应该看到：

- 内阁会议纪要。
- 三省六部内部评审过程。
- 阶段、完整度、字段名、JSON 感文本。
- 与用户灵感无关的通用模板问题。

## 3. 用户画像

目标用户是想做 AI 漫剧、短剧分镜或漫画视频脚本的人。他们可能有一个灵感，但通常缺少：

- 完整剧本能力。
- 导演分镜思维。
- 人物一致性控制能力。
- 提示词工程能力。
- 对生产流程的拆解能力。

他们希望产品帮自己把一句灵感变成可生产资料，而不是让自己先填写复杂表单。

## 4. 核心用户问题

用户输入“学生要跳楼母亲苦苦哀求”这样的灵感时，系统不能直接套悬疑模板，不能机械地问“结尾你想反转还是爽感”。它应该先理解这是一个现实情绪危机场景，然后追问更贴近故事的问题，例如：

- “这场危机里，你更想让观众心疼孩子，还是心疼母亲？”
- “孩子站到天台边缘之前，最想被谁听见？”
- “结尾你想留一点希望，还是更克制一点？”

用户输入“幸福美满的家庭聚餐”时，系统也不能自动套“异常发生、物件证明秘密存在”。它应该判断这是家庭关系、生活剧或反讽方向，再围绕饭桌、关系、隐藏情绪追问。

## 5. 产品原则

### 5.1 对话优先

内阁第一阶段必须像聊天，而不是像填工单。系统每一轮只输出一段自然回应和最多一个关键问题。

### 5.2 用户灵感优先

所有追问必须从用户输入中来。不能使用与灵感无关的通用模板。

### 5.3 后台协作隐藏

三省六部、角色评审、结构化字段可以在后台运行，但默认不展示给用户。用户只看到故事、问题、选择和下一步。

### 5.4 先确定故事，再进入生产

剧本是后续人物、场景、道具、分镜和提示词的前提。用户没有确认故事前，不进入资产生产。

### 5.5 少问，但问关键

系统不能一次列 3-5 个问题。默认一轮只问 1 个问题；必要时最多 2 个。

### 5.6 不开盲盒

用户必须能先看到故事提案，再决定是否进入生产。不能在用户还不知道故事是什么的情况下直接生成整套资产。

## 6. 用户流程

### 6.1 入口

用户进入 AI 漫剧办公室工作台。

页面展示：

- 灵感输入框。
- 基础可选项：剧集长度、风格方向、目标平台、画面比例。
- 按钮：开始聊故事。

不展示：

- 内阁讨论。
- 阶段条。
- 部门评审。
- 复杂工单字段。

### 6.2 第一轮对话

用户输入灵感后，系统返回：

1. 一段自然理解。
2. 一个关键追问。

示例：

用户输入：学生要跳楼母亲苦苦哀求

系统返回：

“我会先把它当成一个现实情绪危机来写，不急着制造反转。这个故事的核心不是跳楼本身，而是孩子为什么觉得没人能听见他。  
你更想让观众先跟着孩子走，还是先跟着母亲走？”

### 6.3 继续补充

用户回答后，系统继续追问或生成故事提案。

判断规则：

- 如果主角、冲突、情绪方向、结尾倾向已经足够明确，生成故事提案。
- 如果仍然缺关键因果，只问一个最重要的问题。

### 6.4 故事提案

故事提案必须是用户可读的完整内容，而不是字段堆叠。

建议结构：

- 标题。
- 一句话故事。
- 完整故事稿。
- 主要人物。
- 情绪走向。
- 可拍摄的关键画面。
- 仍需用户确认的地方。

故事提案展示时，隐藏前面的推理和后台讨论过程。

### 6.5 用户确认

用户可以选择：

- 确认这个故事，进入生产。
- 继续聊，调整故事。
- 换一个方向重写。

用户确认后，后台再启动三省六部生产链路。

## 7. 功能需求

### 7.1 主创对话官

新增一个用户可见角色：主创对话官。

职责：

- 接收用户灵感。
- 判断故事类型。
- 用自然语言回应。
- 追问最关键的问题。
- 生成故事提案。

主创对话官不等于三省六部中的某一个部门。它是 AI 漫剧办公室的前台创作入口。

### 7.2 后台内阁

后台内阁仍可保留。

职责：

- 检查故事因果。
- 检查人物动机。
- 检查视觉可生产性。
- 检查人物一致性风险。
- 检查是否适合进入后续生产。

后台内阁的结果默认不展示，只转化为：

- 主创对话官下一轮应该问什么。
- 故事提案需要改哪里。
- 是否可以进入生产。

### 7.3 追问生成规则

每轮最多展示一个主问题。

问题必须满足：

- 和用户当前灵感直接相关。
- 使用自然口语。
- 不出现“主冲突”“故事承诺”“设定锁死”等内部术语。
- 不使用编号列表，除非用户明确要求整理。
- 不问用户已经给过答案的问题。

### 7.4 故事生成规则

故事稿必须满足：

- 是完整故事，不是大纲碎片。
- 有开端、发展、高潮、结尾。
- 有明确人物动机。
- 有事件因果。
- 有可拍摄画面。
- 符合用户给出的题材和情绪。

禁止：

- 未经用户要求套悬疑反转模板。
- 使用“异常发生、规则显形、关系反转”等万能框架。
- 把严肃题材写成猎奇内容。
- 输出与用户灵感明显无关的故事。

### 7.5 用户可编辑

故事提案生成后，用户可以针对故事补充一句修改意见。

例如：

- “母亲不要一开始就变好。”
- “结尾不要大团圆。”
- “孩子不是被校园霸凌，而是长期被忽视。”
- “故事更现实一点，不要悬疑。”

系统根据修改意见重新生成故事提案。

## 8. 页面需求

### 8.1 默认状态

页面主要区域是一个对话式创作区。

包含：

- 灵感输入区。
- 对话记录区。
- 当前故事提案区。
- 操作按钮区。

### 8.2 对话记录

对话记录只展示：

- 用户说的话。
- 主创对话官的回应。

不展示：

- 后台角色名。
- 内阁讨论。
- 调试字段。
- JSON 字段。

### 8.3 当前故事提案

故事提案独立成一个柔和的内容区。

展示：

- 标题。
- 一句话故事。
- 完整故事稿。
- 关键人物。
- 关键画面。

故事提案存在后，前面的讨论过程可以折叠。

### 8.4 按钮

建议按钮：

- 继续聊故事。
- 重新写一版。
- 确认故事，进入生产。

不建议按钮：

- 生成内阁意见。
- 查看阶段。
- 完整度分析。

## 9. 模型调用需求

### 9.1 主创对话官模型

主创对话官必须优先使用大模型。规则模板只能用于模型不可用时的兜底。

如果模型不可用，页面需要提示：

“当前没有可用的编剧模型，系统只能使用基础规则生成，效果会比较生硬。请先检查模型配置。”

### 9.2 推荐模型能力

主创对话官需要文本创作能力强的模型。

最低要求：

- 中文长文本能力好。
- 能稳定遵循角色。
- 能理解用户补充上下文。
- 能输出自然对话和完整故事。

### 9.3 后台评审模型

后台评审可以使用较便宜模型或规则兜底，不影响前台体验。

## 10. 数据结构建议

### 10.1 ConversationTurn

字段：

- role：user 或 assistant。
- content：展示给用户的自然语言。
- created_at：时间。

### 10.2 StoryProposal

字段：

- title。
- logline。
- genre。
- tone。
- full_story。
- characters。
- key_scenes。
- unresolved_questions。
- version。

### 10.3 CabinetReview

字段：

- role。
- concern。
- risk。
- suggestion。
- visible_to_user：默认 false。

## 11. 验收标准

### 11.1 基础体验

输入一句灵感后，系统必须像聊天一样回应，而不是输出结构化评审。

### 11.2 问题相关性

追问必须和用户输入强相关。

例如：

- 用户输入家庭聚餐，不应追问“物件证明更大的秘密存在”。
- 用户输入学生跳楼，不应追问“第一集钩子要反转还是爽感”。

### 11.3 问题数量

默认每轮只问 1 个问题，最多 2 个。

### 11.4 故事完整性

生成的故事提案必须是完整故事，不是“每集梗概”或字段列表。

### 11.5 后台隐藏

用户默认看不到“内阁讨论”“阶段”“完整度”“首辅收束”等内部内容。

### 11.6 模型失败提示

模型不可用时，用户必须知道当前是规则兜底，不应误以为大模型正常工作。

## 12. 非目标

本 PRD 不解决：

- 最终视频生成。
- 剪辑。
- 角色图片一致性训练。
- 分镜图片生成质量。
- 多模态模型选型。
- Word 交付模板细节。

这些属于故事确认后的生产阶段。

## 13. 需要用户确认的问题

1. “主创对话官”这个名字是否合适？是否需要换成更产品化的名字？
2. 对话阶段最多允许问几轮？建议默认 1-3 轮。
3. 故事提案是否需要支持多个版本对比？
4. 严肃题材是否需要默认开启“克制表达”规则？
5. 用户确认故事后，是否允许回退修改故事，还是进入生产后锁定？

## 14. 建议开发顺序

1. 重构前台内阁体验为对话式入口。
2. 增加模型可用性判断和失败提示。
3. 增加主创对话官 prompt 和数据结构。
4. 隐藏后台内阁评审，只保留调试入口。
5. 重写故事提案展示区。
6. 增加故事确认后进入生产的状态边界。
7. 用至少两个样例测试：
   - 学生要跳楼母亲苦苦哀求。
   - 幸福美满的家庭聚餐。


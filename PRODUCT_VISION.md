# 多 Agent 工作台产品愿景

## 核心理解

这个项目最终不是普通聊天机器人，也不是单次报告生成器，而是一个面向复杂任务的多 Agent 工作台。

用户提交的往往不是一个“回答型问题”，而是一个需要持续推进、反复组织材料、跨工具产出结果的项目。

例如：

- 做行业调研时，最终交付给老板的可能不只是文字报告，还需要近期数据、图表、引用来源、第三方平台截图、可下载附件和结论摘要。
- 做 AI 漫剧时，最终交付也不是单张图，而是角色设定、人物一致性参考、分镜、剧情连贯性、镜头脚本、图片/视频生成任务队列和版本管理。

因此，工作台要解决的是“复杂项目的协作生产”，而不是单 Agent 的一次性问答。

## 产品定位

多 Agent 工作台应具备三个核心能力：

### 1. 项目化

每个用户任务应该变成一个项目空间，而不是一次聊天。

项目空间里保存：

- 用户目标。
- 任务计划。
- 每个 Agent 的工作记录。
- 中间资料。
- 文件、图片、截图、图表等产物。
- 最终交付包。
- 可复用的风格、角色、模板和事实库。

为什么重要：

复杂任务需要连续上下文。报告和漫剧这类任务都不是一句 prompt 能稳定完成的，需要项目级记忆和产物管理。

### 2. 多产物

系统不应该只输出一段文字，而要能输出不同类型的资产：

- Markdown/Word/PDF 报告。
- 数据表格。
- 图表。
- 网页截图。
- 图片参考。
- 角色卡。
- 分镜表。
- 脚本。
- 任务执行日志。
- 最终打包目录。

为什么重要：

真实工作交付通常是材料包，不是纯文本。多 Agent 的价值正在于把不同类型产物拆给不同角色处理。

### 3. 可控协作

多 Agent 不能只是后台黑盒自动跑。

用户需要看到：

- 现在谁在工作。
- 正在产出什么。
- 哪一步需要确认。
- 哪些材料被引用。
- 哪些结果可以重做。
- 哪些设定需要锁定。

为什么重要：

复杂任务里，用户不是旁观者，而是导演、编辑和最终负责人。工作台必须支持人在回路中。

## 两个代表场景

### 场景 A：研究报告工作台

用户目标：

> 我要调研某个行业，并形成一份能提交给老板的报告。

理想流程：

1. 规划 Agent 拆解报告结构和需要的数据。
2. 搜索 Agent 查找近期资料和可信来源。
3. 数据 Agent 提取关键数字并整理表格。
4. 图表 Agent 生成趋势图、对比图或结构图。
5. 截图 Agent 获取第三方页面或资料截图。
6. 验证 Agent 检查数据年份、来源、引用和逻辑。
7. 写作 Agent 汇总成报告。
8. 交付 Agent 打包报告、图表、截图和来源清单。

关键产物：

- 报告正文。
- 数据表。
- 图表。
- 来源列表。
- 截图证据。
- 最终下载包。

### 场景 B：AI 漫剧工作台

用户目标：

> 我要做一部 AI 漫剧，保持人物一致性和故事连贯性。

理想流程：

1. 编剧 Agent 生成世界观、人物关系和分集大纲。
2. 角色 Agent 维护角色卡、外貌设定、服装、表情和禁忌变化。
3. 分镜 Agent 把剧情拆成镜头。
4. 画面 Agent 生成每个镜头的图片提示词。
5. 一致性 Agent 检查角色外貌、服装、场景连续性。
6. 修订 Agent 针对不一致镜头重新生成提示。
7. 交付 Agent 输出分镜表、提示词包、图片资产目录和版本记录。

关键产物：

- 世界观设定。
- 角色卡。
- 分集剧情。
- 分镜表。
- 镜头提示词。
- 角色一致性参考。
- 图片/视频资产版本。

## 对现有架构的影响

当前“三省六部”命名仍然有价值，但需要从“单次任务流程”升级到“项目工作台流程”。

这里的“办公室”不是一组新的 Agent，而是一种场景化工作环境。同样的中书、门下、尚书和六部，在不同办公室里承担不同职责、产出不同材料、接受不同验收标准。

例如：

- 研究报告办公室：强调数据、来源、图表、截图、报告交付。
- AI 漫剧办公室：强调角色卡、分镜、人物一致性、剧情连续性、提示词资产。

建议新增或强化这些概念：

- `Workspace`: 项目空间。
- `Artifact`: 任意产物，包含文本、图片、图表、截图、表格、文件。
- `ArtifactType`: 产物类型。
- `StepRun`: 每个步骤的执行记录。
- `Evidence`: 来源、截图、引用和数据出处。
- `StyleMemory`: 风格、角色、格式等可复用约束。
- `HumanCheckpoint`: 需要用户确认的节点。

为什么重要：

一旦平台目标是多 Agent 工作台，核心数据模型就不能只围绕“任务结果 final_report”，而要围绕“项目资产和可追踪生产过程”。

## 下一步工程方向

优先级建议：

1. 引入 `Office`、`Workspace` 和 `Artifact` 数据模型。
2. 先内置研究报告办公室。
3. 让每个任务步骤可以产出多个 artifact，而不是只有 summary/final_report。
4. 给 Web UI 增加项目资产列表。
5. 给研究报告场景先做一个垂直样板流程。
6. 再抽象出可配置工作流，用于漫剧等更复杂场景。

这样可以避免一开始就做“万能多 Agent”，而是先做一个能真实交付复杂材料包的工作台，再逐步泛化。

# 产品需求文档 (PRD)：AI漫剧柜系统优化项目

## 1. 产品概述 (Product Overview)
**AI漫剧柜系统优化项目**旨在解决当前AI漫剧生成工作流中的系统脆弱性与用户体验断层问题。本次优化的核心目的是提升系统在长对话和复杂资产生成过程中的稳定性，降低用户的试错成本，并打通从“故事文本创作”到“结构化图像资产生产”的壁垒，从而为创作者提供一个流畅、无缝且容错率高的AI漫剧创作体验。

## 2. 目标用户 (Target Users)
* **核心用户（UGC创作者）**：希望借助AI工具将脑海中的故事转化为视觉漫剧的网文作者、同人爱好者或独立短剧创作者。
    * *特征*：重度依赖长上下文以保持角色和世界观一致性；对最终画面的结构化控制要求高。
* **尝鲜用户（泛娱乐体验者）**：通过简单交互生成短篇趣味漫剧的用户。
    * *特征*：对错误和系统崩溃零容忍，缺乏解决技术报错的耐心。

## 3. 问题陈述 (Problem Statement)
基于近期的系统重构，我们识别出以下四个亟待解决的核心痛点：
1. **LLM输出中脆弱的JSON格式依赖 (Fragile JSON format dependency)**：系统高度依赖LLM输出标准的JSON以驱动后续工作流。一旦LLM输出包含多余文本、转义错误或格式缺失，整个生成管线即刻崩溃。
2. **无重试机制的Fail-Fast导致的UX摩擦 (UX friction from Fail-Fast without retries)**：当前系统采用“遇错即停”（Fail-Fast）逻辑。任何微小的解析错误都会直接抛出报错并终止流程，导致用户被迫手动重试，严重打断创作心流。
3. **长对话中上下文膨胀及核心设定遗忘 (Context bloat and forgetting in long conversations)**：随着创作深入，对话Token迅速膨胀。由于缺乏记忆管理，LLM频繁遗忘早期设定的核心参数（如角色外貌、世界观规则、特定的画风设定），导致后期生成的资产OOC（Out of Character）。
4. **自由形态故事与结构化资产间的交接断层 (Handoff gap between free-form story and structured assets)**：用户在对话框中生成的发散性故事文本，难以直接转化为包含“分镜、机位、提示词、角色状态”的结构化图像生成指令，导致文本到图像的转化效率低、质量不可控。

## 4. 解决方案说明 (Solution Description)
本次优化将从“底层稳定性”、“交互容错性”与“工作流重塑”三个维度切入：
* **解耦与柔性解析**：引入强大的JSON校验与自我修复机制，减少对“完美格式”的绝对依赖。
* **静默重试与无感容错**：在后端构建自动化的错误捕获与重试循环，对用户隐藏技术性报错。
* **动态上下文管理 (RAG & Memory)**：将核心设定独立存储为“记忆模块”，在生成时动态注入，确保长线一致性。
* **引入“分镜台”中间层**：在故事与绘图之间增加一个结构化的转换台，将自由文本自动拆解为标准分镜资产。

## 5. 核心功能 (Core Features)

以下功能按照优先级（P0为最高）进行排序：

### P0 (MVP 必须包含)
* **智能JSON解析与修复模块 (Robust JSON Parser)**
  * **功能描述**：采用正则表达式提取混杂在自然语言中的JSON块；引入容错解析器（如自动修复缺失的引号、逗号）；对于彻底损坏的JSON，触发LLM进行轻量级的“格式纠正”单次调用。
* **后台静默重试机制 (Silent Retry Engine)**
  * **功能描述**：当遇到解析失败或API超时时，系统自动执行最多3次的后台重试。
  * **UX侧**：前端UI不再直接弹红框报错，而是显示“正在努力修复生成中...”或“系统正在微调分镜...”的安抚性动画。

### P1 (核心体验提升)
* **核心设定记忆保险箱 (Core Settings Memory Vault)**
  * **功能描述**：将“角色卡（Character Card）”、“世界观（World Bible）”与“画风预设”与日常聊天历史剥离。
  * **机制**：日常对话采用滑动窗口（Sliding Window）仅保留最近N轮，而“记忆保险箱”的内容则作为系统提示词（System Prompt）的固定锚点，每次请求强制挂载，彻底解决遗忘问题。
* **“故事->分镜”交接台 (Storyboard Handoff Workspace)**
  * **功能描述**：在用户确认故事文本后，增加一个自动拆解步骤。系统将文本切片，提取出：[场景描述]、[出场角色]、[镜头建议]、[气泡对话]。
  * **交互**：用户可以在这个“交接台”上对拆解出的分镜数据进行微调，确认无误后再批量发送至生图引擎。

### P2 (未来规划/Nice-to-have)
* **自定义交接模板**：允许高级用户自定义结构化资产的输出格式（如直接导出为特定排版软件的XML）。
* **记忆动态更新**：系统自动识别长对话中用户对角色的“设定修改”，并提示用户是否更新到“记忆保险箱”中。

## 6. 用户路径 (User Flows)

**优化后的核心工作流（以一次分镜生成为例）：**
1. **输入阶段**：用户输入故事发展意图（系统自动提取“记忆保险箱”中的核心设定 + 最近几轮对话历史组合发送）。
2. **生成与解析阶段**：
   * LLM返回结果 -> **[系统判断]** 格式是否正确？
   * *是* -> 进入下一步。
   * *否* -> 触发**[智能JSON修复]**。若修复成功，进入下一步；若失败，触发**[静默重试]**（UI提示：“正在优化分镜结构...”）。
3. **交接断层处理阶段**：
   * 系统不直接去生图，而是进入**[交接台]**界面。
   * 将解析出的结果展示为一个个可视化的“分镜卡片”（包含画面提示词、对话、机位）。
4. **资产生产阶段**：用户检查分镜卡片，点击“生成画面”，系统基于结构化资产精准调用图像模型。

## 7. 成功指标 (Success Metrics)
* **系统稳定性**：JSON解析最终成功率（含修复与重试后）提升至 **99%** 以上。
* **用户体验**：因“格式错误”导致的用户可见报错率下降 **95%**；单次会话的平均持续时长（Session Length）增加，表明用户未被Fail-Fast机制打断。
* **内容一致性**：在超过20轮的长对话后，角色设定的保持率（可通过内部评估集抽样测试）提升至 **90%** 以上。
* **转化率**：从“输入故事”到最终“生成图片资产”的漏斗转化率提升 **30%**。

## 8. 假设与限制 (Assumptions and Constraints)
* **成本限制**：静默重试和JSON修复调用会增加大模型API的Token消耗，需监控重试成本，必要时降级使用更便宜的小模型（如专门微调的JSON修复模型）来处理纠错。
* **延迟问题**：重试机制会导致用户的等待时间变长。前端必须有出色的加载态UI设计（Loading State）来缓解用户的焦虑。
* **技术限制**：不同LLM（如GPT-4 vs 较小的开源模型）对JSON指令的遵循能力差异巨大。系统设计需兼容能力较弱的模型。

## 9. 竞品分析 (Competitive Analysis)
* **对比直接对话类产品（如ChatGPT + DALL-E）**：这类产品容易在长对话中遗忘设定，且每次生图具有高度随机性。**漫剧柜的优势**在于引入了“记忆保险箱”和“结构化交接台”，确保了漫剧资产的连贯性与可控性。
* **对比专业节点类工具（如ComfyUI工作流）**：专业工具学习曲线极其陡峭。**漫剧柜的优势**在于通过后台的容错与静默重试机制，将复杂的技术节点隐藏，实现了“傻瓜式”的流畅UX，同时保留了生产级资产的结构化质量。